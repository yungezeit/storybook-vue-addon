import { create } from 'storybook/theming';

// Based on VSCode's Visual Studio Light theme
export default create({
  base: 'light',
  appBg: '#fff',
  appContentBg: '#fff',
  appPreviewBg: '#fff',
  barBg: '#f3f3f3',
});
